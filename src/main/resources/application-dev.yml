spring:
  datasource:
    url: jdbc:mysql://localhost:${MYSQL_DEV_PORT}/${MYSQL_DATABASE}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}

app:
  storage:
    student-pictures: "uploads/students"

api:
  security:
    token:
      secret: 123456

logging:
  level:
    org.springframework.security: DEBUG